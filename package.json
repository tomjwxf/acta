{
  "name": "acta",
  "version": "0.1.0",
  "description": "A contestable, checkable, versioned public record",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0",
    "vitest": "^3.0.0"
  }
}
